import numpy as np

def embed(sentence):
    """
    Input - (String) sentence to be embedded
    out - (numpy.ndarray) shape of (1, feature_dim) embedded vector of a sentence
    """
    return np.random.rand(512, )